# Reference Sources

`yopo_simple_generator/` is a source snapshot of the upstream YOPO-Simple CUDA
depth dataset generator. It is kept to preserve the original safe-pose and
depth-rendering behavior while the ScaleNav data pipeline is adapted.

This directory is not imported by the Python trainer and is not the target
location for the new EPIC route labeler.
