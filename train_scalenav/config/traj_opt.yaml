# IMPORTANT: velocity in testing (modifiable)
velocity: 6.0

# IMPORTANT: vel_max and acc_max in training
# not the actual values in testing, ensure consistency between testing and training, please refer to ../policy/primitive.py
vel_max_train: 6.0
acc_max_train: 6.0

# IMPORTANT: weight of costs for unit speed (can be visualized in tensorboard)
wg: 0.15   # guidance     (approaching the goal)
ws: 10.0   # smoothness   (smaller jerk)
wa: 1.0    # acceleration (smaller acceleration)
wc: 1.0    # collision    (distance to obstacle)
wp: 0.01   # balanced ESDF-like bubble attraction; keep YOPO goal progress dominant
wpath_mse: 0.2   # ordered witness guidance; validated with dense safety sampling
wcenterline: 0.1  # geometric cross-track fit to the witness centerline
wprogress: 0.1    # terminal witness progress shortfall; restores forward motion
wprogress_floor: 0.0  # no hard floor; goal and path progress provide the push
wtangent: 0.25
# Explicit ordering supervision for the 15 candidate primitive scores.
score_ranking_weight: 0.0
score_ranking_margin: 0.05
score_ranking_target_margin: 0.01
safety_ranking_weight: 0.0  # original YOPO score regression; no separate safety ranking
safety_ranking_target_margin: 0.001

# dataset: set image_size = primitive_num × downsampling_factor (×32 for ResNet-18) in each axis
dataset_path: "dataset"
# YOPO-Simple camera input is 160x96; ResNet's 32x downsampling produces
# vertical_num=3 rows and horizon_num=5 columns of trajectory primitives.
image_height: 96
image_width: 160

# route-conditioned input and dense route supervision
route_dataset_version: 2
local_subgoal_distance_m: 10.0
route_bubble_count: 12
route_anchor_distances_m: [1, 2, 3, 4, 5, 6, 8, 10, 14, 18, 24, 30]
route_clearance_clip_m: 3.0
route_corridor_width_cap_m: 2.0
route_corridor_peak_weight: 2.0
route_corridor_scale_m: 0.5  # ESDF-like signed-distance scale for attraction into safety bubbles
route_progress_floor_m: 6.8
route_progress_error_scale_m: 2.0
dense_route_points: 128
dense_route_step_m: 0.25
route_incompatible_score_cost: 10.0  # score-only penalty outside the route goal sector
robot_radius_m: 0.3
safety_margin_m: 0.2
route_start_tolerance_m: 1.0
route_terminal_tolerance_m: 1.5
route_min_execution_length_m: 2.0
route_max_curvature_rad_m: 1.6
route_max_heading_deg: 51.0

# trajectory and primitive param (Note: traj_time = 2 * radio / vel_max) (can be visualized in rviz)
horizon_num: 5
vertical_num: 3
horizon_camera_fov: 90.0
vertical_camera_fov: 60.0
horizon_anchor_fov: 30.0
vertical_anchor_fov: 30.0
radio_range: 5.0    # planning horizon: 2 * radio_range
radio_num: 1        # only support 1 currently

# safety penalties
d0: 1.2
r: 0.6
safety_peak_weight: 1.0
safety_collision_margin_weight: 10.0
# Dense temporal audit for polynomial trajectories; this does not change the
# network's 15 primitives or the 30-point witness path target.
safety_eval_points: 100

# distribution for unit state sampling (can be visualized by 'python ./policy/yopo_dataset.py')
vx_mean_unit: 0.4
vy_mean_unit: 0.0
vz_mean_unit: 0.0
vx_std_unit: 2.0
vy_std_unit: 0.45
vz_std_unit: 0.3
ax_mean_unit: 0.0
ay_mean_unit: 0.0
az_mean_unit: 0.0
ax_std_unit: 0.5
ay_std_unit: 0.5
az_std_unit: 0.3
goal_pitch_std: 10.0  # clip goal_length to: 2 * radio_range; 10% probability [0, 2 * radio_range]
goal_yaw_std: 20.0

# extend the map to avoid trajectory exceeding bounds (especially in z-axis) and treating the sky outside the map as obstacle
map_expand_min: [0, 0, 0.2]
map_expand_max: [0, 0, 6.0]
