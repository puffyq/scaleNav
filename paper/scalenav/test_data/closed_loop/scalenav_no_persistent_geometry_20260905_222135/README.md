# Sliding-Geometry Ablation

Source run: `run_20260905_222135_1771760`

The per-trial launcher logs record `local_sliding_graph=true`,
`local_sliding_graph_radius_m=40.0`, and `geometry_map=SLIDING_WINDOW`.
Semantic processing and segment-hold route memory remained enabled. The copied
`config.json` predates recording these launcher environment fields, so the
mode is documented here from the source console logs.
